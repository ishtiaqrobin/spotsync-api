# PostgreSQL DSN
DSN=postgresql://postgres:postgres@localhost:5432/spotsync?sslmode=disable

# JWT Secret (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your-super-secret-key

# Server Port
PORT=8080